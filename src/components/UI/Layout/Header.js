import { Fragment } from "react";

const Header = (props) => {
  return (
    <Fragment>
      <header>
        <h1>Header Name</h1>
        <button>BUTTON</button>
      </header>
    </Fragment>
  );
};

export default Header;
